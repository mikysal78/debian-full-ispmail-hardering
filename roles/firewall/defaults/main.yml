---
firewall_default_policy_input: "drop"
firewall_allow_ssh_from: []
firewall_allowed_tcp_ports: []
firewall_allowed_udp_ports: []
firewall_allow_icmp_echo: true
# true se l'host prende l'IPv4 via DHCP (es. VPS cloud): vedi il template.
firewall_allow_dhcp_client: false
